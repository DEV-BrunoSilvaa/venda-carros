*{
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    list-style: none;
    font-family: "Libre Caslon Text", serif;
    font-weight: 400;
    font-style: normal;
}
body{
    color: white;
    background-color: #6d00d9;
}
header {
    width: 100%;
    gap: 10%;

}
header img {
    width: 250px;
    margin-left: 5%;
}
header ul{
    list-style: none;
    margin-left: 5%;
    cursor: pointer;
}
header ul li{
    display: inline-block;
    font-size: 20px;
    padding: 18px;
}
section{
    background-image: radial-gradient(#7f3d9e, #6d00d9);
    height: 100vh;
    position: relative;

}
section .list{
    width: 70vh;
    height: 100%;
    margin: auto;
    position: relative;
    transform: translate(100vw);
    transition: 0.5s;
}
.list .item{
    position: absolute;
    transform: translate(100vw);
    transition: 0.5s;
    opacity: 0;

}
.list .active{
    transform: translate(0);
    transition: 0.5s;
    opacity: 1;
}
.list .car-img{
    position: absolute;
    top: 13%;
    left: -20%;
    transform: rotateY('-40%');
    width: 60%;
    transform: rotate(-20deg);
}
.list img {
    width: 200%;
    
}
.list .car-img img{
    transform: rotate(0);
    transition: 0.5s;
    transition-delay: 0.3s;
}
.list .content{
    position: absolute;
    right: -30%;
    width: 70%;
    height: 100%;
    display: flex;
    flex-direction: column;
    justify-content:center ;
    align-items: end;
    gap: 20px;
}
.list .car-informaçao, h2 ,.description .informacao{
    transform: translateX(200px);
    transition: 0.7s;
    transition-delay: 0.3s;
}

.list .car-informaçao{
    font-weight: bold;
}
.list h2{
    font-size: 68px;
    transition-delay: 0.5s;
    line-height: 1em;
}
.list .description{
    color: rgb(255, 255, 255);
    font-size: 16px;
    text-align: right;
    max-width: 400px;
    transition: 0.7s
}
.list .informacao{
    border-radius: 30px;
    height: 35px;
    width: 120px;
    transition-delay: 0.9s;
    text-transform: uppercase;
    cursor: pointer;
    border: 2px solid rgb(213, 6, 255);
    background-color: #80ff09;

}
.list::before{
    content: '';
    position: absolute;
    width: 160%;
    top:43%;
    border-top: 1px solid white;
}
.incicadores{
    position: absolute;
    top: 50%;
    left: 5%;
    transform: rotateX(-50%);
    height: 200px;
    width: 90vw;
    display: flex;
    flex-direction: column;
    justify-content: end;
    gap: 15px;
}
.incicadores .numeros{
    font-size: 60px;
}
.incicadores ul{
    display: flex;
    gap: 10px;
    

}
.incicadores li{
    width: 50px;
    height: 5px;
    background-color: #80ff09;
    transition: 0.5s;
    gap: 10px;

}
.incicadores .ative{
    background-color: rgb(255, 255, 255);
}
.seta{
    width: 70vw;
    position: absolute;
    top: 40%;
    left: 50%;
    display: flex;
    cursor: pointer;
    transform: translate( -50%, -25%);
    justify-content: space-between;
}
.seta .butun{
    top: 80%;
    width: 40px;
    height: 40px;
    cursor: pointer;
    border-radius: 50%;
    border: none;
    transform: 1s;
    
}
.seta .butun:hover {
    background-color: #80ff09;
    cursor: pointer;
}

.seta .butun:nth-child(1) img{
    transform: scale(-1);
}
.seta img{
    width:20px;
    margin-top: 5px;
    cursor: pointer;
}
